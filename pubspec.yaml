name: gg_bridge_dart_typescript
description: >
  Bridge exposing a Dart package as a JavaScript and WebAssembly
  module with hand-typed TypeScript declarations.
version: 1.0.0
repository: https://github.com/tssuite/gg_bridge_dart_typescript.git
publish_to: none

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  web: ^1.1.1
  gg_bridge_dart_side:
    git:
      url: git@github.com:tssuite/gg_bridge_dart_side.git
      tag_pattern: '{{version}}'
    version: ^1.0.1

dev_dependencies:
  gg_console_colors: ^2.1.5
  lints: ^6.1.0
  test: ^1.31.1
